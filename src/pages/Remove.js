import { useState } from "react";
import ReactDOM from 'react-dom';

const Remove = () => {
  return (
    <div style={{
      paddingLeft: "20px"
    }}>
      <h1>Remove Portfolio</h1>
    </div>
  )
}

export default Remove;
